[package]
name = "rtermutils"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]

# These don't count as dependencies, right?
[build-dependencies]
cmake = "0.1.50"
