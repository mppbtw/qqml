#[test]
fn test_parse_multichoice() {
    let input = "
        (1) 'title' {
            * 'correct' (1) -> 'explanation';
            * 'incorrect' -> 'explanation';
            * 'inc';
        };
        ";
}
